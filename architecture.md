# アーキテクチャ概要

## 1. 目的
本システムは、医用画像解析向けのクラウド基盤として、DICOM 画像の正式保管・閲覧と、MATLAB / Python による解析実行環境を統合的に提供することを目的とする。

本システムは以下を重視する。
- DICOM の正式保管と閲覧の整備
- MATLAB GUI 資産を含む既存解析資産の活用
- 頻繁な解析処理に耐える作業導線
- 高コストな計算資源のオンデマンド運用
- 利用者がアプリケーション内で主要操作を完結できること

---

## 2. 採用アーキテクチャ
本システムはハイブリッド構成を採用する。

- DICOM の正式保管・閲覧: **AWS HealthImaging + OHIF Viewer**
- 頻繁な解析処理: **Amazon EC2 上の MATLAB / Python 実行環境**
- GUI 付き MATLAB 資産の実行: **Amazon DCV**
- ワークスペース管理: **Amazon S3 を中心とした論理分離**
- 起動停止制御: **Amazon Q Developer in chat applications + AWS Lambda + AWS Systems Manager**
- 認証: **Amazon Cognito**
- 監査・監視: **CloudWatch / CloudTrail**

この構成により、DICOM 管理基盤と解析ワークロード基盤を責務分離しつつ、ユーザビリティとコスト制御を両立する。

---

## 3. システム全体像

### 3.1 主要コンポーネント
1. **利用者向けアプリケーション**
   - ログイン
   - DICOM 閲覧
   - ワークスペース操作
   - コード編集
   - 設定ファイル管理
   - 解析実行操作
   - 結果確認

2. **DICOM 管理基盤**
   - AWS HealthImaging
   - OHIF Viewer

3. **解析実行基盤**
   - Amazon EC2
   - MATLAB
   - Python
   - Amazon DCV

4. **ワークスペース基盤**
   - Amazon S3
   - 必要に応じて Amazon EFS / FSx for Lustre

5. **運用自動化基盤**
   - Amazon Q Developer in chat applications
   - AWS Lambda
   - AWS Systems Manager
   - EventBridge

6. **認証・監査基盤**
   - Amazon Cognito
   - CloudWatch
   - CloudTrail

---

## 4. 責務分離

### 4.1 DICOM 正式保管・閲覧
責務:
- DICOM 原本の正式保管
- DICOM メタデータ検索
- OHIF によるブラウザ閲覧
- 閲覧権限管理

担当コンポーネント:
- AWS HealthImaging
- OHIF Viewer
- Cognito

### 4.2 解析実行
責務:
- MATLAB / Python コードの実行
- GUI 付き MATLAB プログラムの利用
- 解析結果の生成

担当コンポーネント:
- Amazon EC2
- MATLAB
- Python
- Amazon DCV

### 4.3 ワークスペース管理
責務:
- 個人ファイル領域の提供
- 一時共有領域の提供
- テンプレート共有領域の提供
- 実行結果保存領域の提供

担当コンポーネント:
- Amazon S3
- 必要に応じて Amazon EFS

### 4.4 運用自動化
責務:
- 解析ノードの起動 / 停止 / 状態確認
- 通知
- スケジュール停止
- 一時共有領域の自動削除

担当コンポーネント:
- Amazon Q Developer in chat applications
- AWS Lambda
- AWS Systems Manager
- EventBridge

### 4.5 監査・監視
責務:
- 起動停止ログ
- 画像アクセスログ
- 解析実行ログ
- エラーログ
- メトリクス監視

担当コンポーネント:
- CloudWatch
- CloudTrail

---

## 5. データフロー

### 5.1 DICOM 閲覧フロー
1. 利用者がアプリケーションへログインする。
2. 利用者が対象 DICOM を検索する。
3. OHIF Viewer が HealthImaging へアクセスし、対象画像を取得する。
4. 利用者はブラウザ上で DICOM を閲覧する。

### 5.2 解析実行フロー
1. 利用者がアプリケーション内でコードまたは設定ファイルを選択する。
2. 必要に応じてチャットまたはアプリケーション経由で解析ノードを起動する。
3. EC2 上の MATLAB / Python 実行環境が起動する。
4. 利用者はアプリ内から実行指示を行う。
5. 実行に必要なファイルは `personal/` または共有領域から取得される。
6. 解析結果は `results/` に保存される。
7. 実行ログと状態は監査対象として記録される。

### 5.3 GUI 付き MATLAB 実行フロー
1. 利用者が GUI 付き MATLAB ツールを選択する。
2. 必要に応じて解析ノードを起動する。
3. Amazon DCV セッションへ接続する。
4. 利用者はリモート GUI として MATLAB アプリケーションを操作する。
5. 入出力ファイルはワークスペース領域を介してやり取りする。

### 5.4 一時共有フロー
1. 利用者がファイルを `shared/dropbox/` へ配置する。
2. 他利用者が必要に応じて当該ファイルを参照またはコピーする。
3. 配置から 24 時間後に当該ファイルは自動削除される。
4. 継続利用が必要な場合は `personal/` または `shared/templates/` へ移動する。

---

## 6. ワークスペース設計

### 6.1 論理構造
ワークスペースは以下の論理領域へ分離する。

- `personal/`: 利用者ごとの主作業領域
- `shared/dropbox/`: 一時共有領域（24 時間で自動削除）
- `shared/templates/`: 正式共有テンプレート領域
- `results/`: 実行結果保存領域

### 6.2 各領域の役割

#### personal/
用途:
- 個人のコード
- 個人設定ファイル
- 個人作業データ
- 個人用の一時保存結果

アクセス:
- 原則として本人と管理者のみ

#### shared/dropbox/
用途:
- 一時的な受け渡し
- 一時レビュー
- 中間ファイル共有

アクセス:
- グループまたは全体共有

制約:
- 24 時間で自動削除
- 恒久保管禁止

#### shared/templates/
用途:
- 標準設定ファイル
- 共通 MATLAB / Python テンプレート
- 再利用前提の共有資産

アクセス:
- 読み取り中心
- 更新権限は限定

#### results/
用途:
- 実行結果
- 出力レポート
- 生成画像
- ログ補助ファイル

### 6.3 実装上の初期方針
初期段階では Amazon S3 により論理分離を行う。

例:
```text
workspace/
  users/{user_id}/personal/
  shared/dropbox/
  shared/templates/
  results/{user_id}/
```

必要に応じて、複数計算ノード間の共有作業領域として EFS を追加する。

---

## 7. アプリケーション機能

### 7.1 必須機能
- DICOM の保管と閲覧
- アプリ内コード編集
- 設定ファイルのインポート / エクスポート
- 個人領域 / 一時共有領域 / テンプレート領域 / 結果領域の分離
- MATLAB ノード起動 / 停止 / 状態確認
- Python 実行環境の利用
- 実行結果保存
- 基本ログ記録
- 権限制御

### 7.2 初期リリースでは簡易でよい機能
- 単一ファイル編集
- テンプレート選択
- 実行ログ表示
- 状態通知
- 基本検索

### 7.3 後続フェーズで検討する機能
- 高度な共同編集
- 実行ジョブキュー
- 自動スケールアウト
- 高度検索 UI
- GPU ノード自動切替
- 解析パイプライン自動化

---

## 8. UI / UX 方針

### 8.1 基本方針
利用者の主要操作は、可能な限り単一アプリケーション内で完結できるようにする。

### 8.2 重視する導線
以下の遷移回数を最小化する。
- 画像確認 → コード編集
- コード編集 → 設定選択
- 設定選択 → 実行
- 実行 → 結果確認

### 8.3 初期 UX 方針
- DICOM 閲覧は OHIF で提供する。
- コード編集はアプリ内エディタで提供する。
- GUI 付き MATLAB ツールは DCV 経由で提供する。
- よく使う定型 GUI については将来的な Web 化を検討する。

---

## 9. MATLAB GUI 資産の扱い

### 9.1 初期方針
既存の GUI 付き MATLAB プログラムは、EC2 上の MATLAB で実行し、Amazon DCV により利用者へ提供する。

### 9.2 理由
- 既存資産を大きく改修せずに利用できる。
- MATLAB デスクトップ前提の GUI と整合しやすい。
- App Designer / GUIDE 由来資産の移行負荷を抑えられる。

### 9.3 将来方針
利用頻度が高く、操作が定型化しやすい GUI ツールについては、将来的に Web 化を検討する。

---

## 10. 起動停止運用

### 10.1 方針
高コストな解析ノードは常時起動とせず、必要時のみ起動する。

### 10.2 初期機能
- start
- stop
- status

### 10.3 実現方式
- Amazon Q Developer in chat applications
- AWS Lambda
- AWS Systems Manager Automation

### 10.4 補助運用
- 夜間停止
- アイドル時停止
- 失敗通知

---

## 11. セキュリティ方針
- 認証は Cognito を利用する。
- IAM 権限は最小権限を守る。
- 一般利用者へ EC2 の直接操作権限を与えない。
- ワークスペース領域は論理的かつ権限的に分離する。
- 主要操作は監査可能とする。
- 保存データおよび通信は暗号化を前提とする。

---

## 12. 監査・監視方針
監査対象:
- ログイン
- DICOM アクセス
- ノード起動 / 停止
- 解析開始 / 終了
- 共有領域への配置
- エラー

監視対象:
- EC2 状態
- Lambda エラー
- ストレージ利用量
- 共有領域削除失敗
- 起動時間異常

---

## 13. 初期スコープ
初期リリースでは以下を対象とする。
- DICOM 正式保管
- OHIF による閲覧
- MATLAB / Python 実行ノード
- DCV による GUI 利用
- アプリ内コード編集
- 設定ファイル管理
- `personal/` / `shared/dropbox/` / `shared/templates/` / `results/` の分離
- 起動 / 停止 / 状態確認
- 基本監査ログ

---

## 14. 1か月運用後の見直し対象
- HealthImaging へのアクセス頻度
- アクティブ画像量
- S3 ワークスペース利用量
- EC2 稼働時間
- MATLAB GUI 利用頻度
- 一時共有領域の利用傾向
- EFS 追加の必要性
- ノード分離や複数台化の必要性

---

## 15. 設計上の禁止事項
- HealthImaging を解析用の主ストレージとして扱わない。
- 高額 EC2 を常時起動前提で設計しない。
- `shared/dropbox/` を恒久保管領域として扱わない。
- GUI 前提の MATLAB 資産を無理にサーバレス化しない。
- 監査ログが残らない操作経路を正式機能としない。
- 個人領域と共有領域の責務を混在させない。

